#include "main.h"

/**
 * _memset - function that fills byes in blocks
 * @*s: memory area pointer
 * @b: consant byte
 * @n: bytes of memory
 * Returns: s
 */

char *_memset(char *s, char b, unsigned int n)
{
	unsigned int u;
	s = n;

	for (u = 0; u < s[u]; u++)
	{
		return (b);
	}
}
