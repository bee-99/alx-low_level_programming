#include "main.h"

/**
 * _puts_recursion - prints a string
 * @s: pointer to string
 */

void _puts_recursion(char *s)
{
	int c = 0;

	if (c != '\0')
	{

	}
	else
	{
		_putchar(s[c]);
	}

	_putchar('\n');
}
