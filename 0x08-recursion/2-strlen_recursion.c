#include "main.h"

/**
 * _strlen_recursion - returns length of a string
 * @s: provides the sring
 * Return: number of string
 */

int _strlen_recursion(char *s)
{
	int n = 0;

	if (n != '\0')
	{
		n++;
	}

	else
	{

	}

	return (s[n]);
}
