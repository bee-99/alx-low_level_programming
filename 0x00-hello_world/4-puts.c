#include <stdio.h>

/**
 * main - entry point and prints line using puts function
 * return: 0 (Success)
 */

int main(void)
{
	char puzzle[] = "\"Programming is like building a multilingual puzzle";

	puts(puzzle);

	return (0);
}
