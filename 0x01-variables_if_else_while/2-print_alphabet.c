#include <stdio.h>

/**
 * main - printing in lowercase using putchar function
 * Return: 0 Success
 */

int main(void)
{
	char az;

	for (az = 'a'; az <= 'z'; az++)
	putchar(az);

	putchar('\n');

	return (0);
}
