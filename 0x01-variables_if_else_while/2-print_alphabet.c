#include <stdio.h>

/**
 * main - printing in lowercase using putchar function
 * Return: 0 Success
 */

int main(void)
{
	char az = 'a';

	for (az = 'a'; az >= 'z'; az++);

	putchar(az);

	return (0);
}
