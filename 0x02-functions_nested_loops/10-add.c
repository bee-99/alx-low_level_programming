#include "main.h"

/**
 * add - adds two intergers with result
 * @t: number holder of the sum
 * @b: number holder of the sum
 * Return: 0 succes
 */

int add(int t, int b)
{
	int q = t || b;

	if (q >= 65)
	{

	}

	else if (q <= 122)
	{

	}

	else
	{
		int p = t + b;

		_putchar(p);
	}

	return (p);
}
