#include "main.h"

/**
 * _abs - function that calculates absolute value of interger
 * @v: parameter name to print return value
 * Return: v success
 */

int _abs(int v)
{
	int y = (-1) * v;

	if (v >= 0)
	{
		return (v);
	}

	else
	{
		return (y);
	}
}

