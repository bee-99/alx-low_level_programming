#include "main.h"

/**
 * *leet - replaces according to number
 * @*: relates to char
 * Return: char
 */

char *leet(char *l)
{
	char z[] = "aeotl";
	char y[] = "AEOTL";
	char x[] = "43071";

	l = x;

	if (z != y)
	{
		return(l);
	}

	else if (z == l && y == l)
	{
		return(l);
	}

	return(l);
}

